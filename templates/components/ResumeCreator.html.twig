<div {{ attributes }}>

    <div class="container-fluid p-5">
        <div class="row">
            <div class="form-container col-5">
                {{ form(form) }}
            </div>
            <div class="col-7">
                {{ include('resume/resume_template.html.twig') }}
            </div>
        </div>
    </div>

</div>